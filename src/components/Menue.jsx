import React from 'react';
import "./Menue.css";

const Menue = (props) => {
    const {img, name, alt, type, price} = props;
    return (
        <div className='FoodList'>
            
        </div>
    );
};

export default RirackumaComponent;